{
  "homepage": "http://aarynverma.github.io/flickr-photo-gallery name",
  "name": "flickr-photo-gallery",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "^4.0.3"
  },
  
  "dependencies": {
    "express": "^4.17.1",
    "react": "^17.0.2",
    "react-addons-css-transition-group": "^15.6.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
